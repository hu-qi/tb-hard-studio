# tb-hard Studio

Read `AGENTS.md` first. This is a Harbor/TB2-aligned benchmark authoring repository.

Use the stable `cases/<case_id>/` layout and the Make targets defined in `AGENTS.md`. Do not put private authoring material into `cases/<case_id>/task/` or delivery exports.

For task creation, use the project Skills and Subagents under `.claude/`. For Harbor behavior, prefer the official docs over memory and record any version-sensitive decision under `docs/` or the case brief.
